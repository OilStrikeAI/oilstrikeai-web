import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Get Your Free Discovery Audit — OilStrikeAI",
  description:
    "Upload a PSC, JOA, or JIB statement and get a full, real audit of billing discrepancies and deadlines — free, no credit card required.",
};

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children;
}
